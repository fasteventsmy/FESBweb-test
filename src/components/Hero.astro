---
// Hero with centered black logo and Concept-C accent highlights
---
<section id="hero" class="hero">
  <div class="hero-inner">
    <img
      src="/assets/fast-logo-black.svg"
      alt="Fast Events"
      class="hero-logo"
      width="320"
      height="80"
      decoding="async"
    />

    <h1>
      <span class="line1">Make <span class="accent">moments</span></span>
      <span class="line2">that move people.</span>
    </h1>

    <p class="lead">
      We design, build, and run <span class="accent">brand experiences</span>—from
      launches and roadshows to <span class="accent">immersive worlds</span>. Fast, crisp, on point.
    </p>

    <div class="cta">
      <a href="#services" class="button button--mint">What we do</a>
      <a href="#work" class="button button--ghost">Our work</a>
    </div>
  </div>
</section>

<style>
  /* Layout */
  .hero {
    position: relative;
    min-height: clamp(560px, 70vh, 760px);
    display: grid;
    place-items: center;
    text-align: center;
    color: var(--bg); /* Rich Black (#0A0A0A) */
  }

  /* Light Silver overlay wash (Concept-C) */
  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(245, 247, 250, .78); /* Light Silver #F5F7FA */
    pointer-events: none;
  }

  .hero-inner {
    width: min(1100px, 92vw);
    margin-inline: auto;
    position: relative;
    z-index: 1;
  }

  /* Logo — ~4× bigger overall, responsive */
  .hero-logo {
    height: clamp(120px, 18vw, 200px);
    margin: 0 auto .5rem;
    display: block;
  }

  /* Headline */
  .hero h1 {
    font-family: var(--font-sans);
    font-weight: var(--font-heading-weight, 800);
    letter-spacing: -0.015em;
    line-height: 1.05;
    font-size: clamp(2.8rem, 7vw, 5.2rem);
    margin: 0 0 .3rem;
  }
  .hero h1 .line1,
  .hero h1 .line2 {
    display: block;
  }

  /* Subtitle */
  .lead {
    font-weight: var(--font-body-weight, 500);
    font-size: clamp(1.06rem, 1.8vw, 1.3rem);
    line-height: 1.55;
    margin: .5rem auto 1.25rem;
    max-width: 62ch;
  }

  /* Accent highlight (Default Accent mint) */
  .accent { color: var(--brand-cta); }

  /* CTA row (kept as-is) */
  .cta {
    display: inline-flex;
    gap: .8rem;
  }
</style>
