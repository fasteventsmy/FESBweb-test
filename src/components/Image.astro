const { src, alt="" } = Astro.props;
const PLACEHOLDER = "https://i.imgur.com/fxI7KNH.png";
const finalSrc = src && String(src).trim() ? src : PLACEHOLDER;
---
<img src={finalSrc} alt={alt} loading="lazy" decoding="async" style="width:100%;height:auto;display:block;border-radius:12px;" />
