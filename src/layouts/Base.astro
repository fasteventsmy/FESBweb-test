---
import "../styles/base.css";
/* Pick one theme for now (B is your dark concept).
   We’ll add switching later after the site is live. */
import "../styles/theme-concept-b.css";

import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const { title = "Fast Events" } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <Header />
    <main class="pt-16">
      <slot />
    </main>
    <Footer />
  </body>
</html>
